import React, {FC} from 'react';
import {ICombineCreditsCast} from "../../types/ICombinedCredits";

const CastPerson: FC<ICombineCreditsCast> = ({title, release_date}) => {
    return (
        <div>

        </div>
    );
};

export {CastPerson};
